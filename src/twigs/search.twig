
{% set data = {
  pageId: 'search',
  pageTheme: 'yellow-lemon',
  pageTitle: 'Search & buy!',
} %}

{% extends 'core/template.twig' %}

{% block Content %}

  {{ Header({
    headline: 'Search & buy!',
    search: true,
    searchPlaceholder: 'Find something cool & get it',
  }) }}

  {{ Headline({
    headline: 'All categories',
  }) }}

  <div class="unicorn">
    {{ source('src/assets/images/unicorn.svg')|raw }}
  </div>

{% endblock %}
