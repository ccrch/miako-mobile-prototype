
{% set data = {
  pageId: 'profile',
  pageTheme: 'green-meadow',
  pageTitle: 'Profile',
  subpageId: 3,
} %}

{% extends 'core/template.twig' %}

{% block Content %}

  {{ Header({
    headline: 'Profile',
    text: 'Your purchases',
  }) }}

  {{ Subnavigation({
    pageId: data.pageId,
  }) }}

  {{ Headline({
    headline: 'History of your purchases',
  }) }}

  <div class="unicorn">
    {{ source('src/assets/images/unicorn.svg')|raw }}
  </div>

{% endblock %}
