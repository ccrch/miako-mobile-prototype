
{% set data = {
  pageId: 'favorites',
  pageTheme: 'pink-bubble',
  pageTitle: 'Favorites',
  subpageId: 3,
} %}

{% extends 'core/template.twig' %}

{% block Content %}

  {{ Header({
    headline: 'Favorites',
    text: 'Cool stuff you saved for later',
  }) }}

  {{ Subnavigation({
    pageId: data.pageId,
  }) }}

  {{ Headline({
    headline: 'List of your favorite charities',
  }) }}

  <div class="unicorn">
    {{ source('src/assets/images/unicorn.svg')|raw }}
  </div>

{% endblock %}
