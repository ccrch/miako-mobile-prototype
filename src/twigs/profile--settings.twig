
{% set data = {
  pageId: 'profile',
  pageTheme: 'green-meadow',
  pageTitle: 'Profile',
  subpageId: 6,
} %}

{% extends 'core/template.twig' %}

{% block Content %}

  {{ Header({
    headline: 'Profile',
    text: 'Stuff you sold',
  }) }}

  {{ Subnavigation({
    pageId: data.pageId,
  }) }}

  <p>Settings</p>

  <div class="unicorn">
    {{ source('src/assets/images/unicorn.svg')|raw }}
  </div>

{% endblock %}
